import type { NextPage } from 'next';
import { PageWrapper } from '../shared';

const Home: NextPage = () => {
  return <PageWrapper title="Prepise » Профиль">profile</PageWrapper>;
};

export default Home;
