import {
  setActiveTab,
  setItemData,
  updateItemData,
} from './model/store/profileSlice';
import profile from './model/store/profileSlice';
import { ProfileState } from './model/types/store';

export { setActiveTab, profile, setItemData, updateItemData };
export type { ProfileState };
